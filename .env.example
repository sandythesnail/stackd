# Copy to .env for local builds (npm run build). In Vercel, set these under
# Project Settings > Environment Variables instead — these are public/publishable
# keys (safe to ship to the browser), not secrets.

CLERK_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxxxxxxxxxxxxxx
SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
